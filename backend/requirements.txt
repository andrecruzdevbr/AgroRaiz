# Core Framework
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.7.0
email-validator==2.2.0

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
psycopg2-binary==2.9.10  # driver sync exigido pelo Alembic (env.py roda migrations em modo sync)
alembic==1.14.0

# Redis & Celery
redis[hiredis]==5.2.1
celery[redis]==5.4.0

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.12

# AI
# Optional — only needed when AI_PROVIDER=anthropic
# anthropic==0.40.0  # Optional: only needed when AI_PROVIDER=anthropic

# HTTP
httpx==0.27.2

# Rate limiting
slowapi==0.1.9

# Logging
structlog==25.1.0

# Utils
python-dotenv==1.0.1
tenacity==9.0.0

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.27.2
